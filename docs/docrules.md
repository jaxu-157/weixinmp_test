# 文档规范

## 文档更新规则

1. 每完成一个阶段，在 `todo.md` 记录完成事项和遇到的问题
2. `plan.md` 记录项目整体进度，标记完成状态
3. 重大架构决策需要在文档中记录原因

## 命名规范

- 文件名使用小写+连字符：`my-module.ts`
- 组件使用 PascalCase：`FaultPanel.vue`
- 服务层使用小写：`fault.ts`, `api.ts`

## 代码变更记录

每次较大的代码变更需在 `todo.md` 中记录：

- 日期
- 变更内容
- 影响范围
- 验证结果
